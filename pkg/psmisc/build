VERSION="23.1"
SRCDIR="psmisc-${VERSION}"
TARBALL="${SRCDIR}.tar.xz"
URL="https://sourceforge.net/projects/psmisc/files/psmisc"
SHA256SUM="2e84d474cf75dfbe3ecdacfb797bbfab71a35c7c2639d1b9f6d5f18b2149ba30"
DEPENDS="ncurses"

prepare() {
	return
}

build() {
	run_configure "$1" --disable-selinux
	make -j $NUMJOBS
}

deploy() {
	local SOURCE="$1"
	local DEPLOY="$2"

	make DESTDIR="$DEPLOY" install
	cp "$SCRIPTDIR/pkg/$PKGNAME/rootfs_files.txt" "$DEPLOY"
}

check_update() {
	curl --silent -L $URL | grep -o ">psmisc-[0-9.]*tar.xz<" | \
	    sed 's/>psmisc-//g' | sed 's/.tar.xz<//g' | \
	    verson_find_greatest "$VERSION"
}