prepare() {
	return
}

build() {
	return
}

deploy() {
	local SOURCE="$1"
	local BUILD="$2"
	local DEPLOY="$3"

	mkdir -p "$DEPLOY/dev" "$DEPLOY/sys" "$DEPLOY/proc"
	mkdir -p "$DEPLOY/tmp" "$DEPLOY/var" "$DEPLOY/run"
}
